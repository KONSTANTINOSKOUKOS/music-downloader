import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'music.downloader',
  appName: 'music-downloader',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
